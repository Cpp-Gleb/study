#include "StegLove.h"
#include <iostream>
#include <string>

using namespace  std;

void RecomendTools::ReadHex(string datafile) {
    ifstream infile(datafile, fstream::in);

}

int main(){
    string datafile;
    getline(cin, datafile);
}